- Block ciphers process fixed-size blocks of data, applying the same transformation to each block using the secret key.

---
## How Does a Block Cipher Work?

A block cipher operates on fixed-size blocks of plaintext, applying a series of well-defined mathematical operations such as substitution, permutation, and bitwise operations, which are determined by the secret cryptographic key. The encryption algorithm transforms the plaintext into unreadable ciphertext. During decryption, the same secret key is used to reverse the transformation, converting the ciphertext back into the original plaintext.


## Basic Idea: Confusion + Diffusion

Block ciphers work by repeatedly:
- 🔀 **Substituting** bits (confusion)
- 🔄 **Rearranging / mixing** bits (diffusion)
- This is done in multiple **rounds**.

### 1. Substitution (Confusion)
#### What it means:
- Substitution replaces pieces of data with other values.
- It hides the relationship between:
	- The **key**
	- The **ciphertext**
- So attackers cannot see patterns.
### 2. Diffusion:
##### What it means:
- Diffusion spreads the influence of **one input bit** over many output bits.
- So if you change 1 bit in plaintext:  → many bits in ciphertext change.
- This is called the **Avalanche Effect**.